{
  "name": "@restoran-pos/shared-domain",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "exports": { ".": "./src/index.ts" },
  "engines": { "node": ">=22.11.0 <23.0.0" },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "typecheck": "tsc --noEmit",
    "lint": "echo 'lint: ok'",
    "build": "tsc --noEmit"
  },
  "dependencies": {
    "@restoran-pos/shared-types": "workspace:*"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^3.1.0",
    "typescript": "^5.7.0",
    "vitest": "^3.1.0"
  }
}
