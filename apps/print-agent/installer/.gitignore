dist/
*.wixobj
*.wixpdb
*.msi

# Vendor binary'ler — root .gitignore *.exe ignore'unu override et
!vendor/nssm.exe
# ADR-004 Amd4 — NativeAOT spooler yardımcısı (kaynak: ../spooler-helper/)
!vendor/spooler-raw.exe

# Lokal MSI smoke helper script'leri (geliştirici ortamı, repo'ya commit edilmez)
# Bunlar Session 68 PR-6 lokal install/uninstall doğrulama için yazıldı;
# Phase 4 MSI deterministic build çalışmalarında reuse edilebilir.
install-test.ps1
uninstall-test.ps1
check-state.ps1
